# 08_testing.md

# Testing & Evaluation

## 1. 戦略

| 層 | 手段 | 対象 |
|---|---|---|
| 静的検査 | `npm run lint` / `npm run build` | 全コード。buildで全SSGページ生成が成功すること |
| ユニット | 最小限 | `lib/` のデータアクセス・slug変換・チャンピオン検索 |
| **画面評価（中心）** | **Playwright MCP** | 全画面の機能・プロトタイプとのUI/UX一致 |

## 2. Playwright MCP 評価手順

1. `npm run dev` でローカル起動（`http://localhost:3000`）
2. Playwright MCPでブラウザを開き、ビューポートを設定して各画面を巡回
   - モバイル: **390×844**（プロトタイプ 1a 基準）
   - PC: **1280×800**（プロトタイプ 2a 基準）
   - 参考: 768〜1279px で2カラム圧縮になることを1画面で確認
3. §4 のチェックリストを上から実行し、スクリーンショットを取得
4. 視覚照合: プロトタイプ `docs/design/LoL対面AIアドバイス Metaたろう プロトタイプ.dc.html` をブラウザで開き、対応する `data-screen-label` の画面と見比べる（[06_ui.md](./06_ui.md) §1 の対応表）

## 3. 評価用データ

モックデータ（[03_database.md](./03_database.md) §6）を使用する。

- 対面代表: MID `ahri-vs-annie`（プロトタイプと同一対面）
- データなし代表: MID `yasuo-vs-zed`（意図的に未作成）
- **未対応レーン代表: JG `vi-vs-leesin`**（T-1300 でデータを退避。「データがありません」+ `noindex` になること）

> BOT代表 `jinx-thresh-vs-caitlyn-lulu` は T-1300 で削除した（06_ui §8 / 09 §9 のアーカイブ参照）。

## 4. 受け入れチェックリスト

### 共通（全画面）

- [ ] ヘッダー（ロゴ・ナビ・Patchバッジ）とフッター（免責表記）が表示される
- [ ] 配色・フォントが 06_ui §2 のトークンに一致（ネイビー×ゴールド、Noto Sans JP + Cinzel）
- [ ] 390px で1カラム、1280px でPCレイアウトになる
- [ ] フッターの法務3リンク（利用規約・プライバシーポリシー・免責事項）が実ページ（`/legal/*`）へ遷移する（T-803）
- [ ] **ヘッダーロゴが新ロゴになっている**（Next.js初期の「対」ボックスが残っていない。T-1406）
- [ ] **ブラウザタブのファビコンが新アイコン**（Next.js初期の `favicon.ico` が残っていない。T-1407）

### LP（`/`）

- [ ] ヒーロー見出しとCTAが表示される（**文面は T-1405 のユーザー確定版**）
- [ ] 結果プレビューカード・できること3枚・使い方3ステップが表示される
- [ ] 「使い方」の説明が対応レーン（TOP / MID）と矛盾しない
- [ ] CTAから `/search` へ遷移する
- [ ] OG画像（`opengraph-image`）が新ロゴ版で出力される（T-1407）

### 検索（`/search`）

- [ ] **タブが存在せず1ページで完結する**（T-1401）
- [ ] **レーン5択（TOP/JG/MID/ADC/SUP）が表示され、TOP / MID のみ押せる**
- [ ] **JG / ADC / SUP は「準備中」表示で `disabled`**（クリックしても選択状態にならない）
- [ ] チャンピオン入力で「あに」→ アニー/アニビアが候補に出る（ひらがな検索）
- [ ] カタカナ・英語（`annie`）でも同じ候補が出る
- [ ] 全角英数（`ａｎｎｉｅ`）・半角カナ（`ｱﾆｰ`）でも候補が出る（NFKC正規化）
- [ ] 2体確定で実行ボタンが活性化し、結果ページへ遷移する
- [ ] 2体確定後に「自分と相手の2体が確定すると表示できます」が**消える**（T-809）
- [ ] **入力欄の `font-size` が 16px 以上**（T-1402。`browser_evaluate` で `getComputedStyle` を確認）
- [ ] **390px の実機/エミュレータで入力欄にフォーカスしても自動ズームしない**（§5 の手動確認）

**IMEの近似回帰**（合成compositionイベント。`browser_evaluate` で実施。詳細は §5）:

- [ ] 変換中は `[role="listbox"]` が描画されず、ラベル行に「候補 N件」が出る
- [ ] 変換確定のEnterでチャンピオンが選択されない（Chrome順序 / Safari順序の両方）
- [ ] 確定後の意図的なEnterではチャンピオンが選択される（過剰ブロックの回帰）
- [ ] 変換の前後で入力欄の `getBoundingClientRect().top` が変化しない（390pxで確認）

> 合成イベントで値を入れる際は `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,"value").set`
> 経由にすること。`input.value = x` はReactのvalue trackerに吸収され `onChange` が発火しない。

### 対面結果（`/matchups/mid/ahri-vs-annie`）

- [ ] VSパネル・AI評価（星5段階）と評価パネルの注記が表示される
- [ ] 勝率が**表示されない**（`winRate` 未提供のため。06_ui §7）
- [ ] 要約 / 詳細立ち回り（序盤・中盤・終盤）/ 注意スキル / パワースパイク / 推奨セットが揃っている
- [ ] 用語リンク・用語チップから `/glossary` の該当アンカーへ遷移する
- [ ] `generateMetadata` によるtitle/descriptionとJSON-LDが出力されている
- [ ] 390pxでヘッダー行（‹・対面結果・レーンバッジ・パッチ表記）が折り返さない（T-806）
- [ ] **1280px で情報が重要度順に上から並び、左右への視線往復が発生しない**（T-1403。06_ui §4.3）
- [ ] **390px の縦積み順が 1280px の重要度順と一致する**（T-1403）
- [ ] **表示項目が T-1301 の決定と一致する**（不要と判断した項目が残っていない / 追加項目が出ている。T-1404）

### データなし（`/matchups/mid/yasuo-vs-zed`）

- [ ] 「データがありません」が表示され、検索へ戻れる
- [ ] `<meta name="robots" content="noindex">` 相当が出力されている

### 未対応レーン（`/matchups/jg/vi-vs-leesin`）

T-1300 でデータを退避したURL。**404ではなく「データがありません」+ `noindex` になること**を確認する
（ルートは残しているため。02_architecture §5）。

- [ ] 「データがありません」が表示される
- [ ] `noindex` が出力されている
- [ ] `/sitemap.xml` に含まれない

### 検索履歴

- [ ] 結果閲覧後、`/search` の「最近の検索」に対面チップが表示される
- [ ] 履歴チップのクリックで該当結果へ遷移する
- [ ] 11件以上閲覧すると古いものから消える（直近10件）

### SEO

- [ ] `/sitemap.xml` に全対面ページが含まれる
- [ ] **`/sitemap.xml` に BOT・JG のURLが含まれない**（T-1300）
- [ ] `/sitemap.xml` に法務3ページ（`/legal/terms` `/legal/privacy` `/legal/disclaimer`）が含まれる（T-803）
- [ ] `/robots.txt` が配信される

## 5. 手動確認（Playwrightで再現できない項目）

### IME（Windows日本語入力）

Playwright は文字を直接入力するためOSのIMEを経由せず、§4「「あに」→ アニー/アニビア」は
IMEが壊れていてもパスし続ける。**以下は実際にIMEで入力して目視確認する**（06_ui §6.1）。

- [ ] MS-IMEで「あに」を入力中（未確定の状態）、候補ドロップダウンが表示されないこと
- [ ] 変換確定のEnterでチャンピオンが選択されてしまわないこと
- [ ] 確定直後に候補ドロップダウンが「アニー / アニビア」で開くこと
- [ ] 変換中のEscapeがIMEの変換取り消しとして働き、入力欄が空にならないこと
- [ ] 変換中に「候補 N件」がラベル行に表示されること
- [ ] Google日本語入力でも同じ結果になること

> 近似的な自動回帰は Playwright MCP の `browser_evaluate` で合成イベントを発火して行える
> （`compositionstart` 後に `isComposing: true` の Enter を送り、選択が発生しないことを確認）。
> ただしOSのIMEウィンドウ描画は再現できないため、重なりの確認は目視に限る。

### モバイルの自動ズーム（T-1402）

Playwright のビューポート設定では iOS Safari のフォーカス時ズームを再現できない。**実機またはiOSシミュレータで確認する。**

- [ ] チャンピオン入力欄をタップしてもページが拡大しないこと
- [ ] 拡大しないまま候補ドロップダウンが選択できること
- [ ] **ピンチズームは引き続き可能なこと**（`maximum-scale=1` で殺していないことの確認。06_ui §4.2）

### データ生成パイプライン（P7以降）

- [ ] `npm run data -- validate --all` がエラーゼロ（`data/` を変更するPRでは必須）
- [ ] **品質評価コマンドがゴールドセットで合格ラインを満たす**（P13 の T-1305 実装後。09 §8）

## 6. 合格基準

- チェックリスト全項目パス（§5 の手動確認を含む）
- プロトタイプとの視覚差分が「意図的な差（実データ・実URL・勝率非表示等）」のみであること
- `npm run build` の出力で対面ページが静的（Static / Partial Prerender）と表示されること
